# Loaded into the pfsnitch anchor by the watchdog when the daemon dies and
# pfsnitch_failmode is "open".
#
# This exists because the anchor being EMPTY now means "blocked": pf.conf
# blocks outbound and relies on this anchor to re-open it. So failing open can
# no longer be done by flushing - it has to be done by loading this.
pass out all keep state
